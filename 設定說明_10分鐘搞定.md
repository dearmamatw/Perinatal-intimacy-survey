# 自動送出版｜設定說明（約 10 分鐘，不用會寫程式）

你會做三件事：開一個試算表 → 貼一段程式碼並發布 → 把發布拿到的網址填回問卷。照順序做就好。

---

## 第一步：開一個空白試算表

1. 用你的 **dearmama.tw Google 帳號** 登入。
2. 到 Google 雲端硬碟 → 新增一個空白的 **Google 試算表**。
3. 命名為「孕期親密關係問卷_回覆」。這個表就是所有答案的落點，一行一人。

---

## 第二步：貼程式碼並發布

1. 在那個試算表上方選單，點 **擴充功能 → Apps Script**。會開一個新分頁，裡面有一個空白的 `Code.gs`。
2. 把裡面預設的東西**全部刪掉**。
3. 打開我給你的 `後端程式碼_貼到AppsScript.gs`，把**整份內容複製貼上**進去。
4. 按上方磁碟片圖示 **儲存**。
5. 右上角點 **部署 → 新增部署作業**。
   - 齒輪「選取類型」選 **網頁應用程式（Web app）**。
   - 「說明」隨意填，例如 `問卷後端 v1`。
   - **執行身分**：選「我（你的信箱）」。
   - **誰可以存取**：一定要選 **「所有人」**（Anyone）。← 這步沒選對，問卷會送不出去。
   - 按 **部署**。
6. 第一次會跳出授權，點 **授權存取 → 選你的帳號**。如果出現「Google 尚未驗證這個應用程式」，點 **進階 → 前往（不安全）**——這是你自己寫的程式，安全。
7. 完成後會給你一段 **網頁應用程式網址**，長得像
   `https://script.google.com/macros/s/AKfy..../exec`
   **把它複製起來。**

> 想確認有沒有成功：把這段網址直接貼到瀏覽器打開，看到「OK - 問卷後端運作中」就對了。

---

## 第三步：把網址填回問卷

1. 用純文字編輯器打開 `孕期親密關係_時光走廊問卷.html`。
2. 搜尋 `PASTE_YOUR_APPS_SCRIPT_URL_HERE`（只有一處）。
3. 把它換成你剛剛複製的網址，記得保留兩側的單引號。像這樣：
   ```
   const SUBMIT_URL = 'https://script.google.com/macros/s/AKfy..../exec';
   ```
4. 存檔。完成。

---

## 測一次

用手機打開那個 HTML，隨便填幾題、走到最後按送出。回去看你的試算表，應該多出一行。第一行會自動長出所有題目的標題。

---

## 幾個你會想知道的事

- **改題目怎麼辦？** 如果之後你增刪題目，`HTML 的 labelMap`、`HTML 的 buildRow`、`程式碼的 HEADERS` 這三個地方的欄位名要一起改，否則會對不到欄。需要時我幫你改。
- **資料想畫成圖表？** 直接在同一份試算表用「插入 → 圖表」，就能把量表分數畫成你的「性慾地形圖」。
- **要多人維護？** 把試算表分享給小牛醫師（檢視或編輯權限），她就看得到即時回覆。
- **關於「不記名」的誠實用詞：** 這份設計不收姓名/email/IP，但資料會存到雲端試算表、且有填答時間戳。所以對外請用「不記名」而非「完全匿名」——問卷開頭的說明已經幫你改成經得起檢驗的版本了。
